<% provide(:title, 'Add Client') %>
<h1>Add a New Client</h1>

<div class="row">
  <div class="span6">
    <%= simple_form_for(@client, :html => { class: 'form-horizontal' }) do |f| %>
    <%#= render 'shared/error_messages' %>
      <%= f.input :name %>
      <%= f.button :submit, label: 'Add', class: "btn btn-primary" %>
    <% end %>

  </div>
</div>
