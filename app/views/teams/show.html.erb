<% provide(:title, @team.name) %>
<h1><%= @team.name %>'s Team</h1>

<table class="table table-striped">
	<thead>
		<th>Name</th>
		<th>Email</th>
		<th>Title</th>
		<th>Team</th>
	</thead>

	<tbody>
		<%= render @users %>
	</tbody>
</table>